extends layout

block content
    .container-fluid
        .image-container
            img.img-fluid(src=product.images[0], alt='Responsive image')
        hr#dividerStyle
        h1 #{product.name}
        h3 #{price}
